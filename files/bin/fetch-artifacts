secrets-starting-with syndromepress | while read item
do
    mkdir -p /workspace/wp/${item}
    wp-cli core download \
        --allow-root \
        --no-color \
        --path=/workspace/wp/${item} \
        --locale=en_US
done