# This file is auto generated by conf-update! Do not modify

# If wp-config does not exist
if [ ! -f /workspace/wp/{{getenv "item"}}/wp-config.php ]; then
    wp-cli config create \
        --path=/workspace/wp/{{getenv "item"}} \
        --dbname=${DB_NAME:-wordpress} \
        --dbuser=${DB_USER} \
        --dbpass=${DB_PASS} \
        --dbhost=${DB_HOST:-localhost} \
        --dbprefix=${DB_PREFIX:-wp_}
else
    echo "Success: wp-config.php already exists!"
fi

{{range gets "/config_raw/*"}}
wp-cli config set --path=/workspace/wp/{{getenv "item"}} {{base .Key}} {{.Value}} --raw
{{end}}

{{range gets "/config/*"}}
wp-cli config set --path=/workspace/wp{{getenv "item"}} {{base .Key}} {{.Value}}
{{end}}
